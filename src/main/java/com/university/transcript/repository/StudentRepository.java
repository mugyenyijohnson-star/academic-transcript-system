package com.university.transcript.repository;

public interface StudentRepository {
    // Define methods for data persistence
    // For example:
    // void save(Student student);
    // Student findById(Long id);
    // List<Student> findAll();
    // void delete(Long id);
}